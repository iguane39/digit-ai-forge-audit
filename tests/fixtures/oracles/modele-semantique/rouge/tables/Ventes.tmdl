table Ventes

	measure 'Montant HT' = SUM(Ventes[montant_ht])

	column id_commande
		dataType: int64

	column client_sk
		dataType: int64

	column livreur_sk
		dataType: int64

	column montant_ht
		dataType: decimal

	partition Ventes = m
		source = "silver.ventes"
